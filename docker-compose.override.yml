# Override file - extends docker-compose.yml
# Only add additional config, don't duplicate labels

services:
  frontend:
    networks:
      - traefik-public
      - ersen-network

networks:
  traefik-public:
    external: true
  ersen-network:
    driver: bridge
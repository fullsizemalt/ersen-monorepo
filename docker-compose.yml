version: '3.8'

services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: daemon
      POSTGRES_USER: daemon
      POSTGRES_PASSWORD: ${DB_PASSWORD:-daemon_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - daemon-network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      PORT: 3000
      DATABASE_URL: postgresql://daemon:${DB_PASSWORD:-daemon_password}@db:5432/daemon
      NODE_ENV: production
      JWT_SECRET: ${JWT_SECRET}
      FRONTEND_URL: ${FRONTEND_URL}
      WORKOS_API_KEY: ${WORKOS_API_KEY}
      WORKOS_CLIENT_ID: ${WORKOS_CLIENT_ID}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      STRIPE_PRICE_STANDARD: ${STRIPE_PRICE_STANDARD}
      STRIPE_PRICE_PRO: ${STRIPE_PRICE_PRO}
      SPOTIFY_CLIENT_ID: ${SPOTIFY_CLIENT_ID}
      SPOTIFY_CLIENT_SECRET: ${SPOTIFY_CLIENT_SECRET}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    depends_on:
      - db
    networks:
      - daemon-network
      - traefik-public
    restart: unless-stopped
    command: sh -c "npm run db:migrate && npm run db:seed && npm start"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.daemon-api.rule=Host(`daemon.runfoo.run`) && (PathPrefix(`/api`) || PathPrefix(`/auth`))"
      - "traefik.http.routers.daemon-api.priority=100"
      - "traefik.http.routers.daemon-api.entrypoints=websecure"
      - "traefik.http.routers.daemon-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.daemon-api.loadbalancer.server.port=3000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
    networks:
      - daemon-network
      - traefik-public
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.routers.daemon-web.rule=Host(`daemon.runfoo.run`)"
      - "traefik.http.routers.daemon-web.priority=10"
      - "traefik.http.routers.daemon-web.entrypoints=websecure"
      - "traefik.http.routers.daemon-web.tls.certresolver=letsencrypt"
      - "traefik.http.services.daemon-web.loadbalancer.server.port=80"

networks:
  daemon-network:
    driver: bridge
  traefik-public:
    external: true

volumes:
  postgres_data:
